- hosts: odroid
  vars:
    - dev_install: True
  roles:
    - setup
    - role: audio
      audio_card: 'hifishield2'
    - mopidy
    - role: docker-compose
    - role: dev_env
      when: dev_install
